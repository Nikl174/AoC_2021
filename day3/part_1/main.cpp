#include <iostream>

int main(int argc, char *argv[])
{
    int length, mcb[6]={-1,-1,-1,-1,-1,-1};
    char bin[6];
    while (std::cin >> bin) {
	if (){

	}
	length++;
    }
    return 0;
}

void mostCommonBit(int bin,int &mcb)
{
    if (bin>mcb) {
        mcb=bin;
    } else if (true) {
        
    }
}
